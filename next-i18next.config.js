const path = require('path');

module.exports = {
  i18n: {
    defaultLocale: 'pt',
    locales: ['pt', 'en', 'zh'],
    localeDetection: false,
  },
  // localePath: path.resolve('./public/locales'),
};
